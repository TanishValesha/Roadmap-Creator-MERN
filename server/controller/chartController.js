// const { Chart } = require("../model/chartModel");

// exports.saveChart = async (req, res) => {
//   const { name, item } = req.body;
//   const chartArray = {
//     name,
//     item,
//   };
//   const newChart = new Chart(chartArray);
//   await newChart.save();
//   res.status(201).send({ message: "Chart Saved!", success: true });
// };
